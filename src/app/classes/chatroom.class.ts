export class Chatroom {
  name: string;
  desc: string;
  code: string;

  constructor(name: string, desc: string, code: string) {
    this.name = name;
    this.desc = desc;
    this.code = code;
  }
}
